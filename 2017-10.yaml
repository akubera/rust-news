---
title: October Rust News
date: October 11, 2017
author: Andrew Kubera

slides:
  - title: "Version 1.21! 🎉"

  - title: "v1.21 Features"
    bullets:
      - 'Literal References now static'
      - >
          ```
            fn main() {
              let x = &5;

              thread::spawn(move || {
                println!("{}", x); });
            }
          ```

  - title: "v1.21 Features"
    bullets:

      - 'Rust language server via rustup'
      - - '`rustup component add rls-preview`'

      - 'RFC for const-generics (∏-types) accepted'
      - - Associate integers with type `Matrix<i32, 3, 5>`
        - 'Not implemented yet'

  - title: "v1.21 Features"
    bullets:
      - 'Reduced compiler memory footprint'
      - 'Iterator::for_each'
      - - '`(0..100).for_each(|x| println!("{}", x));`'
      - '`Ord` max & min functions '

  - title: "v1.21 Features"
    bullets:
      - 'intrinsics::needs_drop'
      - - Returns true if the actual type given as T requires drop glue; returns false if the actual type provided for T implements Copy.

  - title: "v1.21 Features"
    bullets:
      - '`std::mem::discriminant`'
      - - 'Get enum type without using match'
        - >
          ```
          discriminant(&Foo::A("bar")) == discriminant(&Foo::A("baz"))
          ```

  - title: "v1.21 Features"
    bullets:
      - '`Rc<T>` & `Arc<T>` implement...'
      - - '`From<Vec<T>>'
        - '`From<&[T]>` where T: Clone'
        - '`From<str>` `From<String>`'

  - title: "Misc"
    bullets:
      - 'Red Hat adds rust to Developer Toolset'

  - title: "Confrences"
    bullets:
      - 'RustBeltRust : Oct 26-27 : Columbus'
      - 'RustFest : April : Paris'

  - title: "Fin."
